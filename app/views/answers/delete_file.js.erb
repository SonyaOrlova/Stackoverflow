var mainEl = document.querySelector('.main');
var notificationEl = mainEl.querySelector('.notification');

if (notificationEl) mainEl.removeChild(notificationEl);

var answerFilesEls = mainEl.querySelectorAll('.answer__files');

answerFilesEls.forEach(answerFilesEl => {
  answerFilesEl.removeChild(answerFilesEl.querySelector(`#answer-file-${<%= @file.id %>}`));

  if (!answerFilesEl.children.length) answerFilesEl.remove();
});

mainEl.insertAdjacentHTML('afterbegin', '<%= render "shared/success", content: "File was successfully deleted" %>');
